# Configurações extras do vsftpd para a atividade Guardião Proxy.
# Estas linhas são anexadas ao /etc/vsftpd.conf pelo setup-ftp.sh
# (de forma idempotente - só insere o que ainda não estiver presente).
local_enable=YES
write_enable=YES
pasv_enable=YES
pasv_min_port=40000
pasv_max_port=40100
